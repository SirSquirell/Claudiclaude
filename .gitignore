node_modules/
*.log
.DS_Store
dist/
*.zip
screenshots/
# Real HAR captures and un-redacted responses must never be committed.
fixtures/real/
*.har

# Account exports. These carry real holdings and amounts and must never be
# committed, not even redacted — the audit tool reads them from wherever they
# already are.
degiro-portfolio-*.json
*-export.json
exports/

# Local list of names the leak check should refuse. Never committed: a list of
# names kept to guard against names in commits is the thing it guards against.
.leakwords
